import { describe, expect, it } from "vitest";
import { loadBotConfig, loadWorkerConfig } from "../src/config.js";
describe("configuration", () => {
  it("requires token and allowlist", () => { expect(() => loadBotConfig({})).toThrow("TELEGRAM_BOT_TOKEN is required"); expect(() => loadBotConfig({ TELEGRAM_BOT_TOKEN: "x" })).toThrow("ALLOWED_TELEGRAM_USER_IDS is required"); });
  it("deduplicates allowed senders", () => expect([...loadBotConfig({ TELEGRAM_BOT_TOKEN: "x", ALLOWED_TELEGRAM_USER_IDS: "1, 2, 1" }).allowedTelegramUserIds]).toEqual(["1", "2"]));
  it("uses RAG and audit defaults and validates limits", () => { const config = loadWorkerConfig({}); expect(config.agentMaxSteps).toBe(5); expect(config.ragEmbeddingDimension).toBe(768); expect(config.ragChunkOverlapChars).toBe(300); expect(config.ragMaxDistance).toBe(1); expect(config.tokenAuditEnabled).toBe(true); expect(config.tokenAuditProfile).toBe("optimized"); expect(config.chatHistoryTokenBudget).toBe(1_600); expect(config.execModelOutputMaxChars).toBe(3_000); expect(config.ragModelContextMaxChars).toBe(4_500); expect(config.tokenAuditInputUsdPer1M).toBe(0); expect(() => loadWorkerConfig({ AGENT_MAX_STEPS: "11" })).toThrow("AGENT_MAX_STEPS must be an integer from 1 to 10"); expect(() => loadWorkerConfig({ RAG_CHUNK_SIZE_CHARS: "100", RAG_CHUNK_OVERLAP_CHARS: "100" })).toThrow("RAG_CHUNK_OVERLAP_CHARS"); expect(() => loadWorkerConfig({ TOKEN_AUDIT_PROFILE: "fast" })).toThrow("TOKEN_AUDIT_PROFILE"); expect(() => loadWorkerConfig({ TOKEN_AUDIT_ENABLED: "yes" })).toThrow("TOKEN_AUDIT_ENABLED"); expect(() => loadWorkerConfig({ TOKEN_AUDIT_AGENT_ID: "unsafe label" })).toThrow("TOKEN_AUDIT_AGENT_ID"); expect(() => loadWorkerConfig({ TOKEN_AUDIT_INPUT_USD_PER_1M: "-1" })).toThrow("TOKEN_AUDIT_INPUT_USD_PER_1M"); });
});
